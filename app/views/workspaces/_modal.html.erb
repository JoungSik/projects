<div id="workspaceModal" data-modal-target="modal" data-action="click->modal#close"
     class="hidden fixed inset-0 flex items-center justify-center bg-black/75 z-45">
  <%= render "workspaces/form", workspace: Workspace.new %>
</div>